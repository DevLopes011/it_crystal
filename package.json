{
  "devDependencies": {
    "serverless-offline": "^14.4.0",
    "serverless-python-requirements": "^6.1.2"
  },
  "scripts": {
    "debug": "SET SLS_DEBUG=* && node --inspect .\\node_modules\\serverless\\run.js offline -s dev --httpPort 3000"
  }
}
